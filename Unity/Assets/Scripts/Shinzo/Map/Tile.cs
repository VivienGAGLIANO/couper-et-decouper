using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tile : MonoBehaviour
{
    
   /*  void Start()
    {
        // On fixe la position de la tile

        var intx = Mathf.RoundToInt(transform.position.x);
        if (intx % 4 == 1 || intx % 4 == -3)
        {
            transform.position = new Vector3(intx - 1, transform.position.y, transform.position.z);
        }
        else if (intx % 4 == 2 || intx % 4 == -2)
        {
            if (intx <= transform.position.x)
            {
                transform.position = new Vector3(intx + 2, transform.position.y, transform.position.z);
            }
            else
            {
                transform.position = new Vector3(intx - 2, transform.position.y, transform.position.z);
            }
        }
        else if (intx % 4 == 3 || intx % 4 == -1)
        {
            transform.position = new Vector3(intx + 1, transform.position.y, transform.position.z);
        }
        else
        {
            transform.position = new Vector3(intx, transform.position.y, transform.position.z);
        }

        var intz = Mathf.RoundToInt(transform.position.z);
        if (intz % 4 == 1 || intz % 4 == -3)
        {
            transform.position = new Vector3(transform.position.x, transform.position.y, intz - 1);
        }
        else if (intz % 4 == 2 || intz % 4 == -2)
        {
            if (intz <= transform.position.x)
            {
                transform.position = new Vector3(transform.position.x, transform.position.y, intz + 2);
            }
            else
            {
                transform.position = new Vector3(transform.position.x, transform.position.y, intz - 2);
            }
        }
        else if (intz % 4 == 3 || intz % 4 == -1)
        {
            transform.position = new Vector3(transform.position.x, transform.position.y, intz + 1);
        }
        else
        {
            transform.position = new Vector3(transform.position.x, transform.position.y, intz);
        }

    }  */

    
}
